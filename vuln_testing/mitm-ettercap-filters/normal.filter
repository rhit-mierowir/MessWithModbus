mode = 2;
if (ip.proto == TCP && tcp.src == 51800 && ip.src == ‘172.16.141.132’) {
	if (DATA.len == 78) {
		if (DATA.data + (DATA.len – 5) == “\x02\x00\x01\x00\x01”) {
			mode = 1;
		} else if (DATA.data + (DATA.len – 5) == “\x02\x00\x00\x00\x01”) {
			mode = 0;
		}
	}
}

if (ip.proto == TCP && tcp.src == 5020 && ip.src == ‘172.16.141.129) {
	if (DATA.len > 3 && DATA.len == 76) {
		if (mode == 1) {
			DATA.data + (DATA.len - 3) = "\x02\x01\x01";
		} else if (mode == 0) {
			DATA.data + (DATA.len – 3) = “\x02\x01\x00”;
		} else {
			# Do nothing & check again.
		}
	}
}

